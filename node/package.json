{
  "name": "node",
  "version": "1.0.0",
  "description": "Node tutorial",
  "main": "server.js",
  "scripts": {
    "start": "export NODE_ENV=pro || set \"NODE_ENV=pro\" && nodemon server.js",
    "starttest": "export NODE_ENV=dev || set \"NODE_ENV=dev\" && nodemon server.js",
    "test": "export NODE_ENV=dev || set \"NODE_ENV=dev\" && mocha ./api/**/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "author": "Manuel Jesus Jaen Lopez",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "workerpool": "^9.0.4"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  }
}
